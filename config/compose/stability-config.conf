// Compose stability config: treat the :core:model DTOs as stable. They are immutable data
// classes; the compiler only infers them unstable because of their List/nullable fields, and
// the module is deliberately Compose-free so it cannot carry @Immutable itself. Declaring them
// stable lets every composable that takes one skip on structural equality.
com.erendogan6.havatahminim.model.**
